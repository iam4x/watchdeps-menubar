import { generateConstants } from '../utils';

export default generateConstants([
  'PREFERENCES_UPDATE',
  'PREFERENCES_RESET',

  'PROJECT_CREATE',
  'PROJECT_REMOVE',
  'PROJECT_REFRESH(ASYNC)',
  'PROJECT_UPDATE(ASYNC)'
]);
